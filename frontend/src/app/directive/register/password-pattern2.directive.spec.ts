import { PasswordPattern2Directive } from './password-pattern2.directive';

describe('PasswordPattern2Directive', () => {
  it('should create an instance', () => {
    const directive = new PasswordPattern2Directive();
    expect(directive).toBeTruthy();
  });
});
